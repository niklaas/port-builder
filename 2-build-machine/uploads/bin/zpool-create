#!/bin/sh

ZPOOL=tank

# Creates the initial ZFS pool (only needed when attaching *new* EBS)
zpool create $ZPOOL /dev/xbd5

# Imports already created zpool
zpool import $ZPOOL

# Exports zpool
zpool export $ZPOOL

# vim:set ft=sh:
